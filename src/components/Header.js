import { Typography } from "@mui/material";

function Header() {
    return (
        <header>
            <Typography variant="h1" align="center" mt={8}>
                Todo!
            </Typography>
        </header>
    );
}

export default Header;
